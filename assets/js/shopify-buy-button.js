document.addEventListener('DOMContentLoaded', function () {
  var mount = document.getElementById('shopify-fab');
  if (!mount) return;

  mount.innerHTML = '<a class="shopify-fab" href="products.html#shopify-buy-button" title="Shopify Store">Shopify</a>';
});
